# Database Configuration
DB_HOST=db
DB_NAME=customerdb
DB_USER=customeruser
DB_PASSWORD=customerpass

# SendGrid Configuration - REPLACE WITH YOUR ACTUAL VALUES
# 1. Sign up at https://sendgrid.com/
# 2. Go to Settings > API Keys
# 3. Create new API key with "Mail Send" permissions
# 4. Verify your sender email in SendGrid (Settings > Sender Authentication)
# 5. Replace the values below with your actual key and verified sender email
SENDGRID_API_KEY=SG.your-sendgrid-api-key-here
SENDGRID_FROM_EMAIL=your-verified-email@yourdomain.com
SENDGRID_FROM_NAME=Your Company Name

# MySQL Root Password (for Docker Compose)
MYSQL_ROOT_PASSWORD=rootpassword